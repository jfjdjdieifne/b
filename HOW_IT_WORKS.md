# كيف يعمل مرصد السوق؟

## المسار الافتراضي السريع

`run.py` أو `web_app.py` يستعملان `SnapshotAnalyzer`، ولا يحتاجان نموذجاً لغوياً أو مفتاح OpenRouter:

```text
اختيار الزوج + المنصة + فريم التنفيذ
  → جلب Entry TF من المنصة
  → تثبيت نفس المنصة
  → جلب 15m + 4H + 1D
  → حذف كل شمعة غير مغلقة
  → حساب structure / displacement / FVG / OB / liquidity
  → تقاطع Daily و4H
  → فحص نماذج الدخول شرطاً شرطاً
  → READY أو WATCHLIST أو NO_TRADE
  → TP1 + TP2 موثق، أو TP1 + OPEN_TRAILING
```

## معنى الفريم الذي يختاره المستخدم

المستخدم لا يبني التحليل عشوائياً. يختار **فريم التنفيذ فقط**:

- `1D`: انحياز وهدف سيولة كبير — تلقائي.
- `4H`: سياق ومناطق — تلقائي.
- `15m`: سيولة وهيكل الجلسة — تلقائي.
- `5m/3m/1m`: دقة التنفيذ — اختيار المستخدم، والافتراضي 5m.

## حالات القرار

### `READY_NOW`
لا تظهر إلا إذا:

1. Daily و4H متفقان.
2. نموذج دخول مسمى تحققت شروطه.
3. يوجد كسر حديث على فريم التنفيذ مع displacement.
4. السعر عند المنطقة.
5. توقيت النموذج فعال.
6. كل البيانات شموع مغلقة.

### `WATCHLIST / WAIT_CONFIRMATION`
هناك منطقة أو جزء من setup، لكن شرطاً ما ما زال Pending. لا تُعرض كـLimit Order ولا تُفعل تلقائياً.

### `NO_TRADE`
لا انحياز متفق أو لا نموذج صالح. لا توجد حصة صفقات تجبر النظام على اختراع واحدة.

## Order Block وFVG

- FVG تتبع تعريف الشموع الثلاث.
- عتبة displacement (`1.5×ATR` و`65% body/range`) **سياسة تشغيلية للبوت** وليست رقماً رسمياً موحداً من مايكل.
- OB القابلة للاختيار يجب أن تكون مع displacement وFVG من الاندفاع نفسه، غير مبطلة بإغلاق خلالها، ولم تُختبر أكثر من مرتين.
- الفجوة الممتلئة بالكامل ليست منطقة دخول حية.

## الأهداف

1. **TP1 (50%)**: أقرب مستوى حقيقي صالح وغير مسحوب باتجاه الصفقة.
2. **TP2 (50%)**: هدف HTF أبعد فقط مع تأكيدين مستقلين على الأقل.
3. إذا لا يوجد TP2 قوي: **OPEN_TRAILING** للـ50% المتبقية.
4. R:R تُحسب بعد اختيار المستوى. لا يُمدد الهدف لصناعة 3R.
5. `min_rr` إن استُعمل فهو سياسة قرار للمستخدم، لا طريقة لتحريك السعر.

## المتابعة

`TradeMonitor` يحفظ الحالة في `data/tracked_trades.json`:

```text
watchlist
  → تفعيل يدوي
pending_entry
  → لمس الدخول على شمعة مغلقة
active
  → TP1: إغلاق 50% + SL إلى Break-even
runner
  → TP2 أو trailing خلف HL/LH مؤكد
closed / stopped / tp2_hit
```

إذا احتوت شمعة OHLC نفسها SL وTP ولا توجد tick data، يفترض النظام السيناريو المحافظ: SL أولاً.

## الوقت والتوثيق

كل تحليل وحدث يحمل:

- `audit_id`
- وقت UTC
- `America/New_York` مع DST الحقيقي (UTC-4 صيفاً وUTC-5 شتاءً)
- `Asia/Damascus`
- وقت إغلاق آخر شمعة مستخدمة
- المنصة التي جاءت منها البيانات

## اختيار المنصة

- اختيار صريح (`okx` مثلاً): لا fallback ولا خلط منصة أخرى.
- `auto`: يجرب المنصات حتى تنجح واحدة، ثم يثبتها لكل الفريمات في نفس التحليل.
- الفريم غير المدعوم يفشل برسالة واضحة، ولا يتحول إلى 15m بصمت.

## المسار اللغوي القديم

`main.py` و`MultiPassAnalysis` ما زالا متاحين اختيارياً، لكنهما أبطأ ويحتاجان OpenRouter. أضيفت لهما حمايات Pending/TP/شموع مغلقة، ومع ذلك تبقى مخرجات النموذج اللغوي تفسيراً احتماليّاً لا حقيقة أو Win Rate.

## حدود الصدق

- لا ضمان أرباح.
- لا Win Rate بلا اختبار out-of-sample كامل مع رسوم وانزلاق.
- تكييف Kill Zones للكريبتو فرضية مشروع تحتاج تحققاً مستقلاً.
- لا تُرسل مفاتيح تداول Exchange؛ بيانات الشموع عامة والتطبيق لا يضع أوامر مالية.
