# 📋 رسالة الهاندأوف — مشروع "أجنت صناعة فيديوهات شورت احترافية"

> **آخر تحديث:** 2026-08-16
> **المشروع:** `video-agent/` — أجنت يكتب + ينتج + يرفع فيديوهات شورت عمودية (Faceless) على يوتيوب/تيكتوك/ريلز.
> **اقرأ هالملف كامل قبل ما تلمس أي شي — هو خريطة كل اللي صار ووين وقفنا.**

---

## 1) الهدف من المشروع (وشو طلب صاحب المشروع بالضبط)

صاحب المشروع (عم يحكي عربي، بدو محتوى إنجليزي) طلب أجنت:
- يكتب سكربت، يحولو لفيديو شورت عمودي احترافي **من الصفر**
- **مجاني 100%** — بدون مفاتيح مدفوعة، وبدو يشتغل 24/7 عن بعد
- **"مش شغل مبتدئين"** — قارنو بمستوى Kling/Runway (فيديوهات AI سينمائية)
- يرفع أوتوماتيكي على يوتيوب + تيكتوك
- أهم نقطة طلبها بآخر الجلسة: **"فيو نبرة، مو زقزقة، مونتاج احترافي متل الخلق"**

**الواقع اللي اكتشفناه وقلناه لصاحب المشروع بصراحة:**
- الفيديوهات المولّدة بالكامل من الذكاء الاصطناعي (Sora/Kling/Runway) **مش مجانية** وكل فيديو بيكلف دولارات
- الحل المجاني الاحترافي = أسلوب **Faceless**: صوت AI قوي + **لقطات b-roll حقيقية** + إيديت سينمائي
- جودة الصوت المحلي المجاني (Kokoro/edge) **سقفها أقل** من ElevenLabs/Google — الفرق هو "النبرة" اللي صاحب المشروع حاس فيها

---

## 2) بنية المشروع (Architecture)

```
video-agent/
├── agent/
│   ├── cli.py              # واجهة سطر الأوامر (generate/serve/auto/setup-tts/auth-youtube)
│   ├── pipeline.py         # القلب: topic → script → voice → storyboard → render → mp4
│   ├── script.py           # توليد السكربت (قوالب مدمجة / Gemini مجاني / Pollinations)
│   ├── tts.py              # موزّع محركات الصوت (auto: elevenlabs > google > kokoro > edge)
│   ├── tts_pro.py          # ElevenLabs + Google Cloud TTS (مع توقيت كلمة-بكلمة حقيقي)
│   ├── tts_kokoro.py       # Kokoro-82M (offline، Apache-2.0، بدون مفتاح)
│   ├── voice.py            # edge-tts (بدون مفتاح)
│   ├── timings.py          # بيانات التوقيت المشتركة (WordTiming/SentenceTiming)
│   ├── footage.py          # محرك اللقطات: Pexels / Pixabay / Internet Archive / Wikimedia
│   ├── storyboard.py       # تحويل كل جملة لمشهد + 12 موتيف متحرك (شمس/محيط/مال/دماغ/...)
│   ├── audiofx.py          # توليد موسيقى خلفية برمجياً + mixing/ducking نظيف
│   ├── render/
│   │   ├── engine.py       # رندر التايملاين → ffmpeg (بخطوتين للذاكرة)
│   │   ├── scenes.py       # نظام المشاهد + Canvas (يدعم scale للمعاينة السريعة)
│   │   ├── scenes_footage.py  # رندر اللقطات الحقيقية + تلوين سينمائي (Ken Burns)
│   │   ├── draw.py         # أدوات الرسم (تدرجات/توهج/نصوص/انتقالات)
│   │   └── easing.py       # دوال الحركة
│   ├── scheduler.py        # حلقة ذاتية (مواضيع → توليد → نشر)
│   ├── server.py           # API عن بعد (FastAPI): POST /api/v1/generate → job → download
│   └── upload/             # youtube.py / tiktok.py / meta.py (ريلز)
├── assets/fonts/           # خطوط مدمجة (Montserrat/Inter/Bebas)
├── assets/tts/             # موديلات Kokoro (تُحمَّل بأمر setup-tts)
├── outputs/                # الفيديوهات الناتجة (شوف الجدول تحت)
├── Dockerfile / .github/workflows/autopilot.yml  # نشر 24/7 مجاني
└── requirements.txt / pyproject.toml / .env.example
```

---

## 3) شو اشتغل فعلاً (الوضع الحالي — نتائج مثبتة)

### فيديوهات جاهزة في `outputs/` (كلها سليمة، مفكوكة الترميز بنجاح):

| المجلد | المحتوى | المدة | الجودة | ملاحظة |
|---|---|---|---|---|
| `demo/` | "What if the Sun vanished…" | 59.5s | 1080×1920 كاملة | أول إنتاج، صوت edge |
| `airplane/` | "How Airplanes Actually Fly" | 44.4s | 1080×1920 كاملة | صوت edge |
| `ocean/` | "Why the Ocean is Terrifying" | 49.7s | معاينة 540×960 | صوت edge |
| `money/` | "Money Habits…" | 52.8s | معاينة | صوت edge |
| `brain/` | "Psychology Tricks…" | 45.5s | معاينة | صوت edge |
| `sun_kokoro/` | نفس سكربت الشمس | 53.9s | معاينة | **صوت Kokoro am_onyx** (الأحدث بالصوت) |
| `ft_test/` | اختبار اللقطات الحقيقية | 15.6s | معاينة | **أول فيديو بلقطات حقيقية** (الأحدث بالمونتاج) |
| `voice_compare/` | edge_christopher.mp3 vs kokoro_onyx.mp3 | — | صوت فقط | للمقارنة |

### المميزات المثبتة بالكود:
- ✅ توليد سكربت تلقائي (قوالب إنجليزية مدمجة لـ15+ موضوع، أو Gemini/Pollinations بمفتاح اختياري)
- ✅ محركان صوت بلا مفتاح: edge-tts (سحابي) + Kokoro-82M (محلي، صوت am_onyx عميق ~93Hz)
- ✅ محركان صوت احترافي بالمفتاح: ElevenLabs + Google Neural2 (مكتوبين بالكامل، **مش مختبرين** — يحتاجوا مفتاح)
- ✅ رندر موشن جرافيك: 12 موتيف + انتقالات (zoom/wipe/slide/flash) + كاميرا Ken Burns
- ✅ **رندر لقطات حقيقية** (scenes_footage): تلوين سينمائي teal-orange + S-curve + جرين + Ken Burns
- ✅ كابشن كلمة-بكلمة متزامن (للمحركات اللي بترجع توقيت حقيقي) — بلا تداخل
- ✅ موسيقى خلفية مولّدة برمجياً (بلا حقوق) + ducking سلس تحت الصوت
- ✅ API عن بعد (FastAPI) + مجدول ذاتي + رفع يوتيوب/تيكتوك/ريلز (كود كامل)
- ✅ نشر 24/7 مجاني: GitHub Actions cron + Dockerfile + Oracle Cloud
- ✅ `--scale 2/4` لمعاينة سريعة ~4× أسرع

---

## 4) ⚠️ المشاكل اللي واجهناها وحللناها (أهم جزء — لا تعيد نفس الأخطاء)

### أ) مشاكل الذاكرة OOM (كانت تقتل الرندر)
- **العرض:** ffmpeg ينقتل بـ SIGKILL أثناء الترميز، أو الفيديو يطلع تالف.
- **التشخيص:** dmesg بيوضح `Out of memory: Killed process (ffmpeg)`. الصندوق فيه 1.9GB رام فقط.
- **الحل النهائي (مطبّق في engine.py):**
  1. **رندر بخطوتين**: فريمات خام لملف `.rgb` أولاً → بعدها ffmpeg يشفر فيديو بلا صوت → بعدها mux صوت + faststart بخطوة ثانية.
  2. `-preset veryfast` + `-threads 1` → ذروة الذاكرة نزلت من ~1.2GB لـ ~400MB.
  3. `OPENBLAS_NUM_THREADS=1` إلخ في config.py.
- **درس:** أبداً ما تستخدم `-threads 2` أو preset أثقل من veryfast على أجهزة ضعيفة.

### ب) الفيديوهات تتلف مع `-shortest` + pipe
- **السبب:** لما الصوت أطول من الفيديو والـ pipe مفتوح، ffmpeg بيسكر stdin بدري → ملف تالف.
- **الحل:** `-shortest` فقط في وضع الملفات (raw) مو الـ pipe، مع `verify_video()` بعد الرندر (فحص decode نظيف) و`add_faststart()` بس إذا الفحص نجح.

### ج) 🎯 البقّ الأخطر: الكابشن يخفت ويختفي تدريجياً (هيك كان "التزامن" بايظ!)
- **العرض:** النص بالكابشن كان بيظهر بأول جملة ثم يختفي — والجملة 0 وآخر جملة دايماً "OFF".
- **السبب الجذري (استغرق وقت طويل للعثور عليه):** `paste_sprite` كان يعدّل **ألفا السبريت نفسه المخزّن في الـ cache** كل فريم ×0.92. بعد ~50 فريم النص يصير غير مرئي تقريباً. لإنو الـ caption_panel بيمرر `alpha=0.92` للكلمات غير المميزة، وكان يعدّل السبريت المشترك!
- **الحل:** في `draw.py/paste_sprite`: `sprite = sprite.copy()` قبل أي تعديل ألفا.
- **درس:** لا تعدّل أبداً كائن مرسوم/sprit مخزّن بالكاش — انسخ قبل التعديل.

### د) حجم نص السبريت كان 1.5× أكبر من المطلوب
- `make_text_sprite` كان يرسم بمقياس 2× **بدون تنزيل للحجم النهائي** → نصوص متداخلة وشكل سيء.
- **الحل:** بعد الرسم عالي الدقة، `sprite.resize(س/scale)` (LANCZOS) → مقاس دقيق.

### هـ) فقرات الكابشن كانت تمزج جملتين مع بعض
- "seconds? At first, everything" — فقرة واحدة تضم نهاية جملة + بداية التالية.
- **الحل:** `build_caption_phrases` يستقبل `sentence_times` ويكسر الفقرة عند حدود الجمل، مع `_ends_sentence()` يفحص نهاية الكلمة بـ .!? فعلياً.

### و) الصوت "مضغوط/مزقزق" من الميكساج
- **السبب:** `np.tanh(mix * 1.6)` — ضغط قاسي كان يكبّت الصوت.
- **الحل:** في `audiofx.py/mix_master`: لا ضغط على الصوت إطلاقاً — تطبيع نظيف + ducking ناعم + limiter خفيف فقط للقِمم الحقيقية.

### ز) الصوت "سريع"
- edge كان `+4%` (يسمعوه مستعجل) → صار `+0%`.
- Kokoro الافتراضي صار `speed=0.85` (إيقاع هادئ — قسنا: 1.0→4.7s، 0.9→5.0s، 0.85→~5.6s لنفس الجملة).

### ح) Wikimedia Commons بتحظر بالـ 429
- التحميل من `upload.wikimedia.org` بينحظر من IPs مشتركة.
- **الحل:** Internet Archive (`archive.org/download/...`) كمصدر أساسي بلا مفتاح + Pexels/Pixabay بالمفتاح. وأضفنا retry مع backoff.

### ط) موديلات Kokoro (325MB) بتضيع من الصندوق بين الجلسات
- الحفظ بين الجلسات محدود الحجم؛ الموديل لازم يعاد تحميله بـ `python -m agent.cli setup-tts`.
- **على جهاز صاحب المشروع بيظل محفوظ** — المشكلة بس بصندوق المعاينة.

### ي) حساسية: `rate="0%"` غير صالح لـ edge-tts
- لازم `"+0%"` — أي `"0%"` بيكسر الـ backend. (مصلح في كل مكان)

---

## 5) ملاحظات صاحب المشروع (الرسالة الأخيرة = الأولوية القصوى)

قال حرفياً: **"لسا مافي تزامن كابشن ولسا الصوت سيء جداً عم يزقزق ولسا سريع ولا فيو نبره ولا شي والمونتاج والفيديو بدائي جداً جداً كلشي سيء — بدك تلاقي حلول تانية جذرية لتعمل فيديوهات متل الخلق احترافية"**

ترجمتها للعمل:
1. **الصوت** = أولوية #1 → المفتاح المجاني لـ ElevenLabs أو Google (الكود جاهز 100% في `tts_pro.py`، يحتاج فقط `ELEVENLABS_API_KEY` أو `GOOGLE_TTS_API_KEY` في `.env`). المحلي المجاني (Kokoro/edge) **لن يرضيه** — هذه حقيقة وليست عجزاً تقنياً.
2. **المونتاج** = أولوية #2 → وضع `--visual footage` (لقطات حقيقية + تلوين سينمائي) — مثبت يعمل، يحتاج تحسين:
   - مفتاح Pexels يرفع جودة اللقطات كثيراً (Archive نتائجه متواضعة)
   - تحسين مطابقة الكلمة المفتاحية للجملة (المشاهد بتطلع أحياناً عامة)
3. **التزامن** = أولوية #3 → مع ElevenLabs/Google التوقيت **حقيقي** من المحرك (بالفعل مبرمج في tts_pro.py) — يصلح تلقائياً.

---

## 6) الخطوات الجاية (رواد ما تبلش)

### فوراً (لصاحب المشروع):
1. يسجل بمفتاح ElevenLabs مجاني: https://elevenlabs.io → Profile → API Keys → `ELEVENLABS_API_KEY` في `.env`
2. (اختياري لكن مهم للجودة) مفتاح Pexels: https://www.pexels.com/api/ → `PEXELS_API_KEY`
3. يشتغل:
```bash
cd video-agent
pip install -r requirements.txt
sudo apt-get install ffmpeg        # لو مش موجود
python -m agent.cli setup-tts      # لو بدو kokoro كـ fallback
python -m agent.cli generate "Why the Ocean is Terrifying" --visual footage --scale 2 --out outputs/preview
# ولما يرضى بالنتيجة: --scale بدون قيمة = جودة كاملة 1080×1920
```

### هندسياً (تحسينات مقترحة):
1. **اختبار tts_pro.py بمفتاح حقيقي** — لم يُختبر بعد! احتمال أخطاء بسيطة (صيغة الاستجابة، fields) — اقرأ استجابة ElevenLabs JSON الأول وثبّتها.
2. **تحسين keyword → clip**: حالياً `scene_keyword()` بسيط (كلمة أولى). جرّب ترجمة أو map أفضل، أو دمج كلمتين ("sunset clouds").
3. **موهنة اللقطات**: اختيار clip أطول + loop + crossfade بين المشاهد حتى تكون الانتقالات سينمائية.
4. **مزامنة اللقطات مع إيقاع الصوت**: cut على beats/كلمات قوية.
5. **رفع حقيقي ليوتيوب**: الكود جاهز (`upload/youtube.py` + `auth-youtube`) — يحتاج OAuth مرة وحدة من جهاز صاحب المشروع.
6. **ترتيب أولوية موتيفات storyboard** — قائمة الـ keywords فيها تداخل (مثال: "glow in the dark" تاخد sun قبل dark) — راجع `_motif_for`.

---

## 7) كيف تشغّل كل شي (ملخص سريع)

```bash
python -m agent.cli generate "الموضوع"                      # فيديو (motion)
python -m agent.cli generate "الموضوع" --visual footage     # فيديو بلقطات حقيقية
python -m agent.cli generate "الموضوع" --scale 2            # معاينة سريعة
python -m agent.cli generate "الموضوع" --tts elevenlabs     # صوت احترافي (بالمفتاح)
python -m agent.cli serve --port 8000                       # API عن بعد
python -m agent.cli auto "الموضوع"                          # توليد + رفع (لـ cron)
python -m agent.cli setup-tts                                # تحميل Kokoro
python -m agent.cli auth-youtube                             # ربط يوتيوب (مرة وحدة)
python -m agent.scheduler --interval 6h                      # حلقة ذاتية 24/7
```

**API:**
```bash
curl -X POST localhost:8000/api/v1/generate -H 'Content-Type: application/json' \
     -d '{"topic":"Why the Ocean is Terrifying","tts":"elevenlabs","visual":"footage","scale":2}'
# → {"job_id": "..."}  ثم GET /api/v1/jobs/{id} حتى done → نزّل الفيديو
```

---

## 8) ملاحظات/تحذيرات مهمة للمستلم

1. **الصندوق (sandbox) بيرجع بصفر بين الجلسات**: ffmpeg وpip packages والـ 325MB موديل بتضيع. كل جلسة: `sudo apt-get install -y ffmpeg` + `pip install -r requirements.txt` + (لو بدك kokoro) `python -m agent.cli setup-tts`. **لكن `outputs/` و`agent/` و`assets/fonts/` بتظل محفوظة** (ما عدا assets/tts الكبيرة).
2. **الرندر بالجودة الكاملة بطيء**: دقيقة فيديو ≈ 15-25 دقيقة على 2 نوى. استخدم `--scale 2` للمعاينة وخلص عملك قبل ما تشغّل الجودة الكاملة بالخلفية (start_process).
3. **لا تعدّل السبريتات المخزنة** — أبداً (شوف المشكلة ج).
4. **`RENDER_MODE=raw`** هو الافتراضي الآمن للذاكرة — لا تغيّره بدون سبب.
5. صاحب المشروع عم يتكلم **عربي** بدو محتوى **إنجليزي** — اكتبلوا بالعربي وخلّي الفيديوهات إنجليزي.
6. **الصدق أولاً**: لا توعد بجودة ElevenLabs بدون المفتاح. اشرح الخيارات، واعرض جاهزية الكود.
7. حصل نقاش مهم: صاحب المشروع طلب أصلاً حلول "مفتوحة المصدر/مكتبات GitHub" — الجواب الصحيح اللي اعتمدناه: **Kokoro** (مفتوح، Apache-2.0) كمحرك محلي + **ElevenLabs/Google** بالمستوى المجاني كجودة إنتاجية. إذا بدك البقاء مفتوح 100% وبلا مفاتيح، Kokoro هو أفضل المتاح على CPU (أكده بحث 2026: Kokoro = الأفضل جودة/سرعة بلا GPU).

---

## 9) شجرة القرار النهائية

```
بدك فيديو شورت احترافي؟
├── عندك مفتاح ElevenLabs/Google → tts_pro (نبرة + تزامن حقيقي) 🏆
├── ما عندك مفتاح → kokoro (أحسن محلي) أو edge
└── المونتاج:
    ├── عندك مفتاح Pexels → لقطات سينمائية 🏆
    ├── بدون مفتاح → Internet Archive (لقطات حقيقية CC)
    └── بدون نت → motion graphics (12 موتيف)
```

---

**كلمة أخيرة:** المشروع مبني بشكل صحيح (منظّم، موثّق، الاختبارات اللي فشلت اتحلّت). اللي نقص هو **المفتاح المجاني للصوت** اللي يفتح مستوى النبرة، و**تحسين انتقاء اللقطات**. خلي صاحب المشروع يعطيك المفتاحين (10 دقائق تسجيل) وشغّل `--visual footage --tts elevenlabs` وبيشوف الفرق مباشرة.
